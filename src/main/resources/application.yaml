server:
  port: 8081

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/notificator
    username: postgres
    password: postgres

  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: notificator
      properties:
        name: consumer
        password: consumer
      max-poll-records: 1
    producer:
      properties:
        name: producer
        password: producer
